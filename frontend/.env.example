# Local dev: leave empty — Vite proxies /api to http://localhost:8080
# VITE_API_BASE_URL=

# Production (Netlify): public URL of your deployed Spring Boot API (no trailing slash)
# VITE_API_BASE_URL=https://your-backend.onrender.com
